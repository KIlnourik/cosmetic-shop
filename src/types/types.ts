export type CareType = {
  name: string,
  title: string,
  items: object,
};

export type SkinType = {
  name: string,
  title: string,
  items: object,
};

export type ProductType = {
  title: string,
  value: string,
};

export type ProductCareType = {
  name: string,
  title: string,
}
