export type FilterType = {
  name: string,
  title: string,
  items: object,
}

export type CareType = {
  name: string,
  title: string,
  items: object,
};

export type SkinType = {
  name: string,
  title: string,
  items: object,
};

export type City = {
  lat: number;
  lng: number;
  zoom: number;
};
